{{#if readonly}}
  {{input
    type=type
    value=(readonly value)
    readonly="readonly"
    required=required
    placeholder=placeholder
  }}
{{else}}
  {{input
    type=type
    value=value
    required=required
    placeholder=placeholder
  }}
{{/if}}