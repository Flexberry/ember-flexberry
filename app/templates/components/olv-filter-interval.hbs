<div
  class="olv-filter-interval"
  style={{filterIntervalStyle}}
>
  {{#if (eq dynProps.type "date")}}
    <div class="field field--relative">
      {{component componentName
        value=from
        placeholder=fromPlaceholder
        dynamicProperties=dynProps
      }}
      <i
        role="button"
        class="remove icon field__clear-btn"
        title={{t "components.object-list-view.clear-filter-in-column"}}
        {{action "clearFrom"}}
      ></i>
    </div>
    <div class="field field--relative">
      {{component componentName
        value=to
        placeholder=toPlaceholder
        dynamicProperties=dynProps
      }}
      <i
        role="button"
        class="remove icon field__clear-btn"
        title={{t "components.object-list-view.clear-filter-in-column"}}
        {{action "clearTo"}}
      ></i>
    </div>
  {{else}}
    <div class="field">
      {{component componentName
        value=from
        placeholder=fromPlaceholder
        dynamicProperties=dynProps
      }}
    </div>
    <div class="field">
      {{component componentName
        value=to
        placeholder=toPlaceholder
        dynamicProperties=dynProps
      }}
    </div>
  {{/if}}
</div>