<h3>Order - edit form</h3>

{{#if errorMessages}}
  {{#each currentError in errorMessages}}
	{{ui-message
	  type = 'error'
	  closeable = true
	  visible = true
	  title = "Error occured"
	  message = currentError
	}}
  {{/each}}
{{/if}}
<form class="ui form" role="form">
  <div class="field">
    <div>
      <button type="submit" class="ui primary button" {{action 'save'}}>Save</button>
      <button type="submit" class="ui primary button" {{action 'close'}}>Close</button>
    </div>
  </div>
  <div class="ui grid {{if model.errors.shipName 'error' ''}}">
	<div class="ui two wide column right aligned middle aligned content">
		<label>ShipName</label>
	</div>
	<div class="ten wide column">
		<span style="color:red">{{model.errors.shipName}}</span>
		{{input type="text" placeholder="(no value)" required=true value=model.shipName}}
	</div>
  </div>
  <div class="ui grid {{if model.errors.shipCountry 'error' ''}}">
	<div class="ui two wide column right aligned middle aligned content">
		<label>ShipCountry</label>
	</div>
	<div class="ten wide column">
		<span style="color:red">{{model.errors.shipCountry}}</span>
		{{input type="text" placeholder="(no value)" required=true value=model.shipCountry}}
	</div>
  </div>
  <div class="ui grid {{if model.errors.orderDate 'error' ''}}">
	<div class="ui two wide column right aligned middle aligned content">
		<label>OrderDate</label>
	</div>
	<div class="ten wide column">
		<span style="color:red">{{model.errors.orderDate}}</span>
		{{bootstrap-datepicker placeholder="(no value)" value=model.orderDate
			format="dd.mm.yyyy" todayBtn="linked" clearBtn=true orientation="top auto" autoclose=true todayHighlight=true}}
	</div>
  </div>
  <div class="ui grid {{if model.errors.employeeID.firstName 'error' ''}}">
	<div class="ui two wide column right aligned middle aligned content">
		<label>Employee FirstName</label>
	</div>
	<div class="ten wide column">
		<span style="color:red">{{model.errors.employeeID.firstName}}</span>
		{{input type="text" placeholder="(no value)" required=true value=model.employeeID.firstName}}
	</div>
  </div>
  <div class="ui grid {{if model.errors.employeeID.lastName 'error' ''}}">
	<div class="ui two wide column right aligned middle aligned content">
		<label>Employee LastName</label>
	</div>
	<div class="ten wide column">
		<span style="color:red">{{model.errors.employeeID.lastName}}</span>
		{{input type="text" placeholder="(no value)" required=true value=model.employeeID.lastName}}
	</div>
  </div>
</form>
