import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:new-platform-flexberry-services-lock', 'Unit | Adapter | new-platform-flexberry-services-lock');

test('it exists', function(assert) {
  let adapter = this.subject();
  assert.ok(adapter);
});
