import { moduleFor, test } from 'ember-qunit';

moduleFor('route:new-platform-flexberry-services-lock-edit', 'Unit | Route | new-platform-flexberry-services-lock-edit');

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
